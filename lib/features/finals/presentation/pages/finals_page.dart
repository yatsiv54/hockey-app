import 'package:flutter/material.dart';

class FinalsPage extends StatelessWidget {
  const FinalsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Final scores (placeholder)'));
  }
}

